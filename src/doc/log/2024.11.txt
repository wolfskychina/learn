2024.11.1
dijkstra算法
因为有向图没有负的边，所以依赖关系是确定的，根据dijkstra算法的性质，一个节点在距离最近值可以确定的时候，它的
前序节点一定是已经全部被访问完了（也就是前序节点的最小值均已确定）。
1.743信号从一个节点传递到网络中其他所有节点所需要的最少时间。如果无法传递到所有节点，返回-1.经典dijkstra算法；
2.3112访问消失节点的最少时间。在普通的dijkstra算法上加上了节点消失的限制条件，如果节点可能访问到的最小时间超出
了消失时间，那么这个节点无法被访问到，无法入列也就意味着从该节点出发的单条路径无效。