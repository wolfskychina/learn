-- 用户平均活动次数 {sql:easy}
select if(round(count(distinct(session_id))/count(distinct(user_id)),2) is null,0,round(count(distinct(session_id))/count(distinct(user_id)),2)) as average_sessions_per_user from Activity where activity_date between '2019-06-28' and '2019-07-27' ;